@page "/"

<div class="vertical-stack-layout account-page">
    @if (Account != null)
    {
        <AccountCardView Name="@Account.Name"
                         Balance="@Balance" />

        <div class="label">Transactions</div>

        <TransactionsView Transactions="@Transactions" />
    }

</div>

